printLn("Welcome to the X Programming Language!")

func factorial(n int)
    assign mut count: int = n;
    assign mut sum: int = 0;
    while (count < 1) then
        
        count = count - 1
    end
    return printLn("Hi");
end